import type { NextPage } from 'next';

import { Layout } from '../components/atomic/template/Layout';

const Terms: NextPage = () => {
  return (
    <Layout title="利用規約 | わんだーくろーす">
      <h1>Termsページ</h1>
    </Layout>
  );
};

export default Terms;
